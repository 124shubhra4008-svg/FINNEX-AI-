fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.7.0
PyJWT>=2.8.0
python-multipart>=0.0.9

# Optional AI providers (only needed if you set the matching API key)
google-generativeai>=0.7.0
openai>=1.35.0

# Optional local OCR fallback for receipt scanning if you don't want to use
# Gemini/OpenAI vision (requires the Tesseract binary installed separately)
pytesseract>=0.3.10
pillow>=10.3.0
